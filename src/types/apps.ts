export enum localMode {
  ua = "ua",
  en = "en",
}
export enum themeMode {
  dark = "dark",
  light = "light",
}

export enum localStorageKeys {
  theme = "@theme",
  current_user = "@user",
  users = "@users",
}
